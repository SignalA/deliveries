# 输入插件v0.2.9联调包

目标：A40i ARMv7 Linux，动态加载器 `/lib/ld-linux.so.3`。

解压后停止旧输入服务，再执行：

```sh
chmod +x scripts/*.sh bin/arm-linux-gnueabi/*
./scripts/run_service.sh usb-sunxi-ehci-1.2 usb-sunxi-ehci-1.1
```

实际物理口请以 `/proc/bus/input/devices` 为准。默认socket为 `/tmp/gaime-input/input.sock`。
校准应用保持 `HELLO calibration 1`，无需修改应用二进制。枪1/档案0使用 `./launch.sh --gun 0 --player 0 --verbose`。
校准期间先扣目标枪，连接会锁定第一把扣枪的物理枪。测试另一把枪时重新启动校准应用。

## 本次更新

- 新枪使用BTN_TOUCH作为扳机；KEY_C映射投币，KEY_Q映射PAUSE，KEY_SPACE映射换弹（兼容GUN_A事件）。
- `HELLO calibration 1/2` 均发送 `GUN_DOWN <player> <x> <y>`，不再向版本1发送MOUSE_DOWN。
- 接受带gun=0/1、player=0..3的CALIB_RESULT；新版应用填写count=32，表示24组采样和8组目标坐标。插件同时兼容旧值count=24。
- 插件按协议转发通过格式和范围校验的校准结果；校准质量由枪端判断，并通过0x0A和index=0x08返回最终状态。
- 按新版HID指南：index=7 选择档案，index=6 上传8帧，等待一次0x0A，再以index=8 确认生效。
- 接受 `PONG 1`，不再返回UNKNOWN_MESSAGE。

## 验证状态

本次多按键协议测试通过；新版程序已在A40i上启动并识别两把枪。修复版两把枪的扳机按下/松开已通过，移动、投币和A键也有现场记录；B键未见原始按键事件。此前版本的校准应用完成一轮，但采样坐标全部相同且无APPLIED回执，不能视为校准成功。
新版枪固件的实际写入与四档切换由接收方联调验证；本地设备对新增命令未回包，不能宣称写枪成功。
只有 `CALIB_STORED 1 APPLIED` 及服务端 `status=APPLIED` 保存记录表示写入确认；应用显示sent不代表写入成功。
此包不包含枪固件、板子镜像或校准应用。

## 游戏切档

游戏完成 `HELLO game 1` 后发送 `SET_PROFILE 0 2`（枪1、档案2），成功回 `PROFILE_SET 0 2`。枪2用gunNum=1。只有收到成功回复才视为切档完成；详见 `docs/PROTOCOL.md`。

## 校准与游戏切换

校准前先结束现有game进程；校准应用退出后至少等待2秒，让输入插件完成0x06写入和index=0x08查询，再调用/g3/start.sh。/g3/start.sh应保证只启动一个game进程，并在启动前设置ulimit -n 4096。
